numero = Math.sqrt(25);
document.write(numero + "<br><br>");

numero = Math.cbrt(25);
document.write(numero + "<br><br>");

numero = Math.max(2,3,24,5,566,22,4,5,6,43,121,3,344,);
document.write(numero + "<br><br>");

numero = Math.min(2,3,24,5,566,22,4,5,6,43,121,3,344,);
document.write(numero + "<br><br>");

/* Generating a random number between 0 and 99. */
numero = Math.random()*99;
/* Rounding the number to the nearest whole number. */
//numero = Math.round(numero);
/* Rounding the number to the nearest whole number. */
numero = Math.floor(numero+1);
document.write(numero + "<br><br>");

/* Rounding the number to the nearest whole number. */
numero = Math.fround(4.46758376538564);
document.write(numero + "<br><br>");

/* Truncating the number. */
numero = Math.trunc(9.8);
document.write(numero + "<br><br>");

/* Assigning the value of PI to the variable numero. */
numero = Math.PI;
document.write(numero + "<br><br>");

/* Assigning the value of the square root of 1/2 to the variable numero. */
numero = Math.SQRT1_2;
document.write(numero + "<br><br>");

/* Assigning the value of the square root of 2 to the variable numero. */
numero = Math.SQRT2;
document.write(numero + "<br><br>");

/* Assigning the value of Euler's number to the variable numero. */
numero = Math.E;
document.write(numero + "<br><br>");

/* Assigning the value of the natural logarithm of 2 to the variable numero. */
numero = Math.LN2;
document.write(numero + "<br><br>");

/* Assigning the value of the natural logarithm of 10 to the variable numero. */
numero = Math.LN10;
document.write(numero + "<br><br>");

/* Assigning the value of the base-2 logarithm of Euler's number to the variable numero. */
numero = Math.LOG10E;
document.write(numero + "<br><br>");

/* Assigning the value of the base-2 logarithm of Euler's number to the variable numero. */
numero = Math.LOG2E;
document.write(numero + "<br><br>");

